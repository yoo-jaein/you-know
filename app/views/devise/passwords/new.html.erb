<div class="container">
    <div style="text-align:center; margin-top: 45px;">
    <a href="/post/index"><img src="/assets/logo.png"  height="50" class="d-inline-block align-top" alt="logo"></a>
  </div>
  
<div class="jumbotron" style="margin-top: 50px; padding:50px; width:500px; margin-left: auto; margin-right: auto; background-color: white; border-radius: 1; box-shadow: 0 6px 12px rgba(0,0,0,.1);">
    <div class="card-content">
      
    <h2 class="text-center" style="font-weight: bold;">비밀번호 재설정</h2>
    <p class="text-center">계정 이메일 주소를 사용해서 <br>비밀번호를 재설정할 수 있도록 도와드립니다.</p>
    <hr class="my-4">
      
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= devise_error_messages! %>
      
        <div class="p-2 field">
           <i class="fa fa-paper-plane"></i>
              Email Address<br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "이메일" %>
        </div>
      
        <div class="p-2 actions">
          <button type="submit" value="Send" name="commit" class="btn btn1 btn-light">메일 보내기</button>
        </div>
      <% end %>
      
      <%= render "devise/shared/links" %>

   </div>
</div>
</div>

